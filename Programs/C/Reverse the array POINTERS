/*
    @author:    John V. Neijzen
    @activity:  Activity 4 - Reverse the array
    @section:   CSA 12 A
    @version:   0.1
*/

/*
    Complete the following code such that it will display the contents of the array in reverse order, using only the pointer variable.
*/
#include <stdio.h>
#define SIZE 5

int main()
{
    int a[] = {2, 7, 13, 1, -9};
    int ctr;
    int *ptr;

    ptr = &a;

    for(ctr = SIZE; ctr>0; ctr--)
        printf("%d ", *(ptr+ctr-1));
}
